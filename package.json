{
  "name": "browser-skill",
  "type": "module",
  "private": true,
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.7.4",
    "typescript": "^5"
  },
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "extension:zip": "npm --prefix extension run zip",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{js,ts,tsx,json,md,yml,yaml}": "prettier --write"
  }
}
